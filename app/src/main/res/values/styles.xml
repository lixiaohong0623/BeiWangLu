<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:homeAsUpIndicator">@drawable/btn_back</item>
        <item name="android:logo">@null</item>
        <item name="android:actionOverflowButtonStyle">@style/myActionBarOverflowButtonStyle</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="myActionBarOverflowButtonStyle" parent="android:Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/btn_add</item>
    </style>

</resources>
